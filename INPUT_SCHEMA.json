{
  "title": "Emoji-Pulse input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
 "country": {
  "title": "Country",
  "type": "string",
  "default": "US",
  "editor": "textfield",
  "description": "ISO-alpha-2 country code, e.g. US, GB, JP."
}
,
    "daysBack": {
      "title": "Days back",
      "type": "integer",
      "minimum": 1,
      "maximum": 30,
      "default": 7,
      "description": "How many days of history the Actor should examine."
    }
  },
  "required": []
}
