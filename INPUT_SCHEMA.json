{
  "title": "Emoji-Pulse input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "country": {
      "title": "Country",
      "type": "string",
      "enum": ["US", "GB", "CA", "AU"],
      "default": "US",
      "description": "Country code for trending-emoji lookup"
    },
    "daysBack": {
      "title": "Number of days",
      "type": "integer",
      "default": 7,
      "minimum": 1,
      "maximum": 30
    }
  },
  "required": ["country"]
}
