{
  "title": "Emoji-Pulse input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "country": {
      "title": "Country",
      "type": "string",
      "default": "US",
      "description": "ISO-2 code of the country whose emoji trends you want."
    },
    "daysBack": {
      "title": "Days back",
      "type": "integer",
      "minimum": 1,
      "maximum": 30,
      "default": 7,
      "description": "How many days of history the Actor should examine."
    }
  },
  "required": []
}
